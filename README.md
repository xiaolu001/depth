# depth

## Install

Install with npm:

```
npm i depth-map-l --save-dev
```

Usage

```
<div id="depth"></div>
```

```
new depth({
    dom: document.getElementById("depth"),
    width: 800,
    height: 600,
    data: data,
    locale: "zh"
});

```
